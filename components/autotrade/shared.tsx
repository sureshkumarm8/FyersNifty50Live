/**
 * The AutoTrade panels' design primitives now live in components/ui/panels.tsx
 * so the Pre-Market screen can share the same visual language. Re-exported here
 * to keep the existing import sites unchanged.
 */
export * from '../ui/panels';
